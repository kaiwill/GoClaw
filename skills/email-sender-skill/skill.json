{
  "name": "email-sender",
  "description": "使用 nodemailer 通过 SMTP 发送邮件",
  "version": "1.0.0",
  "commands": [
    {
      "name": "email_send",
      "description": "【邮件发送工具】通过 SMTP 发送邮件到指定收件人。当用户要求发送邮件时使用此工具，无需其他操作。",
      "kind": "shell",
      "command": "EMAIL_DATA='{{data}}' node index.js",
      "parameters": [
        {
          "name": "data",
          "type": "string",
          "description": "邮件数据（JSON 格式，包含 recipient、subject、body）",
          "required": true
        }
      ]
    }
  ]
}